{
  "version": 1,
  "ports": { "preview": 3000, "additional": {} },
  "scripts": {
    "setup": { "enabled": true, "command": "pnpm install --frozen-lockfile && pnpm prisma generate" },
    "run": { "enabled": true, "command": "pnpm dev" },
    "archive": { "enabled": true, "command": null },
    "check": { "enabled": true, "command": null }
  },
  "mcpServers": []
}
